// Top-level build file. Versions are a mutually-compatible set:
// AGP 8.6.0 + Gradle 8.7 + Kotlin 1.9.24 + Compose compiler 1.5.14 + Compose BOM 2024.06.00.
// AGP 8.6 is the first that supports compileSdk 35 natively (android-35 is the installed platform).
plugins {
    id("com.android.application") version "8.6.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
